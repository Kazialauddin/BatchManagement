@model Project_09.Models.Batch

@{
    ViewData["Title"] = "Delete";
}
<div class="row">
    <div class="col">
        <h3>Are you sure you want to delete this?</h3>
        <div>
            <h4>Batch Details</h4>
            <hr />
            <dl class="row">
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.BatchId)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.BatchId)
                </dd>
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.BatchName)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.BatchName)
                </dd>
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.CourseHours)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.CourseHours)
                </dd>
            </dl>

            <form asp-action="Delete">
                <input type="submit" value="Delete" class="btn btn-danger" />

            </form>
            <a asp-action="Index"><i class="fa fa-undo"></i> Cancel & Back to List</a>
        </div>

    </div>
</div>

@section Styles{
    <environment include="Development">
        <link href="~/lib/font-awesome/css/font-awesome.css" rel="stylesheet" />

    </environment>
}